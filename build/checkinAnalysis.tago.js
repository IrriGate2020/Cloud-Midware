/*
 * TagoIO (https://tago.io/)
 * TagoIO Builder V3.0.3 (https://git.io/JJ8Si)
 * -------------------
 * Generated by     :: rafael
 * Generated at     :: Monday, August 10, 2026 at 11:17 PM Coordinated Universal Time
 * Machine          :: DESKTOP-G3OTV8E <linux> - Node.js v18.19.1
 * Origin file      :: src/analysis/checkinAnalysis.ts <Typescript>
 * Destination file :: ./build/checkinAnalysis.tago.js
 * -------------------
*/

var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __markAsModule = (target) => __defProp(target, "__esModule", { value: true });
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __reExport = (target, module2, copyDefault, desc) => {
  if (module2 && typeof module2 === "object" || typeof module2 === "function") {
    for (let key of __getOwnPropNames(module2))
      if (!__hasOwnProp.call(target, key) && (copyDefault || key !== "default"))
        __defProp(target, key, { get: () => module2[key], enumerable: !(desc = __getOwnPropDesc(module2, key)) || desc.enumerable });
  }
  return target;
};
var __toCommonJS = /* @__PURE__ */ ((cache) => {
  return (module2, temp) => {
    return cache && cache.get(module2) || (temp = __reExport(__markAsModule({}), module2, 1), cache && cache.set(module2, temp), temp);
  };
})(typeof WeakMap !== "undefined" ? /* @__PURE__ */ new WeakMap() : 0);

// src/analysis/checkinAnalysis.ts
var checkinAnalysis_exports = {};
__export(checkinAnalysis_exports, {
  checkinAnalysis: () => checkinAnalysis,
  default: () => checkinAnalysis_default
});
var import_sdk = require("@tago-io/sdk");

// src/analysis/notificationUtils.ts
function normalizeText(value) {
  return String(value || "").normalize("NFD").replace(/[\u0300-\u036f]/g, "").trim().toLowerCase();
}
function looksLikeEmail(value) {
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value);
}
async function resolveRunUserId(resources, recipient) {
  if (!recipient)
    return null;
  const raw = String(recipient).trim();
  if (!raw)
    return null;
  try {
    const user = await resources.run.userInfo(raw);
    if (user?.id)
      return user.id;
    return raw;
  } catch (_) {
  }
  if (!looksLikeEmail(raw) && raw.length >= 20) {
    return raw;
  }
  let page = 1;
  const wanted = normalizeText(raw);
  while (true) {
    const users = await resources.run.listUsers({
      page,
      amount: 100,
      fields: ["id", "name", "email", "tags", "active"]
    });
    const found = (users || []).find((user) => {
      const values = [user.id, user.email, user.name].filter(Boolean).map((item) => normalizeText(item));
      return values.includes(wanted);
    });
    if (found?.id)
      return found.id;
    if (!users || users.length < 100)
      break;
    page += 1;
  }
  return null;
}
async function sendRunNotification(resources, recipient, title, message, context) {
  const userId = await resolveRunUserId(resources, recipient);
  if (!userId) {
    context?.log?.("No Run user found for notification recipient: " + (recipient || "empty"));
    return false;
  }
  try {
    await resources.run.notificationCreate(userId, { title, message });
    context?.log?.("Push notification sent to user " + userId);
    return true;
  } catch (error) {
    context?.log?.("Error sending push notification to " + userId + ": " + error);
    return false;
  }
}

// src/analysis/checkinAnalysis.ts
function normalizeText2(value) {
  return String(value || "").normalize("NFD").replace(/[\u0300-\u036f]/g, "").trim().toLowerCase();
}
function getTagValue(tags, key) {
  const tag = (tags || []).find((item) => normalizeText2(item?.key) === normalizeText2(key));
  if (tag?.value === void 0 || tag?.value === null || tag?.value === "")
    return void 0;
  return String(tag.value);
}
function isGroupDevice(device) {
  return normalizeText2(getTagValue(device.tags, "device_type")) === "group";
}
async function listAllDevices(resources) {
  const devices = [];
  let page = 1;
  while (true) {
    const result = await resources.devices.list({
      page,
      amount: 100,
      fields: ["id", "name", "tags"]
    });
    devices.push(...result);
    if (!result.length || result.length < 100)
      break;
    page += 1;
  }
  return devices;
}
function groupCheckinAlerts(alerts) {
  const bundles = /* @__PURE__ */ new Map();
  for (const alert of alerts) {
    const metadata = alert.metadata;
    if (!metadata || !["checkin", "checkin_central"].includes(metadata.alert_type) || alert.value !== "enabled")
      continue;
    const group = String(alert.group || alert.id);
    const current = bundles.get(group) || [];
    current.push(alert);
    bundles.set(group, current);
  }
  return Array.from(bundles.entries()).map(([group, records]) => {
    const preferred = records.find((record) => record.metadata?.lock === true) || records[0];
    const metadata = { ...preferred.metadata || {} };
    const locked = records.some((record) => record.metadata?.lock === true);
    metadata.lock = locked;
    return {
      group,
      records,
      metadata,
      value: preferred.value,
      locked
    };
  });
}
async function getCommunicationStatus(resources, deviceId, checkinTimeHours) {
  const now = new Date();
  const lastPayload = await resources.devices.getDeviceData(deviceId, {
    variables: ["payload"],
    qty: 1,
    ordination: "descending"
  }).catch(() => []);
  if (!lastPayload?.length) {
    return {
      lastCommunication: void 0,
      hoursOffline: Number.POSITIVE_INFINITY,
      isOffline: true,
      source: "payload_not_found"
    };
  }
  const lastCommunication = new Date(lastPayload[0].time);
  if (Number.isNaN(lastCommunication.getTime())) {
    return {
      lastCommunication: void 0,
      hoursOffline: Number.POSITIVE_INFINITY,
      isOffline: true,
      source: "payload_invalid_time"
    };
  }
  const hoursOffline = (now.getTime() - lastCommunication.getTime()) / (1e3 * 60 * 60);
  return {
    lastCommunication,
    hoursOffline,
    isOffline: hoursOffline >= checkinTimeHours,
    source: "payload"
  };
}
async function deleteAlertRecords(resources, containerDeviceId, ids) {
  for (let index = 0; index < ids.length; index += 100) {
    const batch = ids.slice(index, index + 100);
    await resources.devices.deleteDeviceData(containerDeviceId, { ids: batch });
  }
}
async function updateAlertLock(resources, containerDeviceId, bundle, lock, extraMetadata, context) {
  const ids = bundle.records.map((record) => record.id).filter(Boolean).map(String);
  await deleteAlertRecords(resources, containerDeviceId, ids);
  await resources.devices.sendDeviceData(containerDeviceId, {
    variable: "alertas",
    value: bundle.value,
    group: bundle.group,
    metadata: {
      ...bundle.metadata,
      ...extraMetadata,
      lock
    }
  });
  context.log(`Checkin lock ${lock ? "activated" : "reset"} for alert_group=${bundle.group} records_replaced=${ids.length}`);
}
function resolveCentralTarget(bundle, groupDevice, groupDevicesById, context) {
  const targetId = bundle.metadata.device_id || groupDevice.id;
  const target = groupDevicesById.get(targetId);
  if (!target) {
    context.log(`Checkin alert_group=${bundle.group} target=${targetId} is not a device_type=group central. Skipping.`);
    return null;
  }
  return target;
}
async function processCentralCheckinAlert(resources, containerGroup, targetCentral, bundle, context) {
  const metadata = bundle.metadata;
  const checkinTimeHours = Number(metadata.checkin_time || 0);
  if (!checkinTimeHours || checkinTimeHours <= 0) {
    context.log(`Invalid checkin_time for alert_group=${bundle.group}. value=${metadata.checkin_time}`);
    return;
  }
  const status = await getCommunicationStatus(resources, targetCentral.id, checkinTimeHours);
  const lastInputLabel = status.lastCommunication ? status.lastCommunication.toISOString() : "never";
  const hoursLabel = Number.isFinite(status.hoursOffline) ? status.hoursOffline.toFixed(2) : "never";
  context.log(`Central checkin target=${targetCentral.id}:${targetCentral.name} container=${containerGroup.id}:${containerGroup.name} alert_group=${bundle.group} last=${lastInputLabel} source=${status.source} offline_hours=${hoursLabel} limit=${checkinTimeHours} locked=${bundle.locked}`);
  if (status.isOffline) {
    if (bundle.locked) {
      context.log(`Central checkin already notified for alert_group=${bundle.group}; skipping until central communicates again.`);
      return;
    }
    if (metadata.send_to) {
      const hoursText = Number.isFinite(status.hoursOffline) ? status.hoursOffline.toFixed(1) : "sem historico";
      await sendRunNotification(resources, metadata.send_to, "Alerta: Central Sem Comunicacao", `A central ${targetCentral.name || targetCentral.id} esta sem comunicar ha ${hoursText} horas (limite: ${checkinTimeHours}h).`, context).catch((error) => context.log(`Error sending central checkin notification: ${error}`));
    }
    await resources.devices.sendDeviceData(containerGroup.id, {
      variable: "alert_triggered",
      value: "Comunicacao da Central",
      metadata: {
        alert_type: "checkin_central",
        alert_variable: "checkin",
        alert_variable_label: "Comunicacao da Central",
        device_id: targetCentral.id,
        device_name: targetCentral.name || targetCentral.id,
        hours_offline: Number.isFinite(status.hoursOffline) ? status.hoursOffline : null,
        checkin_time: checkinTimeHours,
        last_input: status.lastCommunication?.toISOString() || null,
        timestamp: new Date().toISOString()
      }
    });
    await updateAlertLock(resources, containerGroup.id, bundle, true, {
      last_notified_at: new Date().toISOString(),
      last_notified_last_input: status.lastCommunication?.toISOString() || "never"
    }, context);
    return;
  }
  if (bundle.locked) {
    await updateAlertLock(resources, containerGroup.id, bundle, false, {
      last_recovered_at: new Date().toISOString(),
      last_recovered_last_input: status.lastCommunication?.toISOString() || null
    }, context);
  }
}
async function checkinAnalysis(context, scope) {
  context.log("Starting Checkin Analysis - Checking central communication");
  const resources = new import_sdk.Resources({ token: context.token });
  try {
    const allDevices = await listAllDevices(resources);
    const groupDevices = allDevices.filter(isGroupDevice);
    const groupDevicesById = new Map(groupDevices.map((device) => [device.id, device]));
    context.log(`Found ${groupDevices.length} device_type=group central(s) to check`);
    for (const groupDevice of groupDevices) {
      const alertsData = await resources.devices.getDeviceData(groupDevice.id, {
        variables: ["alertas"],
        qty: 9999
      }).catch(() => []);
      const checkinBundles = groupCheckinAlerts(alertsData || []);
      context.log(`Central container ${groupDevice.id}:${groupDevice.name} has ${checkinBundles.length} central checkin alert group(s)`);
      for (const bundle of checkinBundles) {
        const targetCentral = resolveCentralTarget(bundle, groupDevice, groupDevicesById, context);
        if (!targetCentral)
          continue;
        await processCentralCheckinAlert(resources, groupDevice, targetCentral, bundle, context);
      }
    }
    context.log("Checkin analysis completed");
  } catch (error) {
    context.log(`Error in checkin analysis: ${error}`);
  }
}
var checkinAnalysis_default = new import_sdk.Analysis(checkinAnalysis, { token: "a-bae808ee-4460-4042-a1e7-8e9f27ff2624" });
module.exports = __toCommonJS(checkinAnalysis_exports);
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  checkinAnalysis
});
